<div class="author">
    <h1 class="author_heading"> Book Blog</h1>
    <p class="author_body">Explore from our huge selection of books to add to your reading list. Embark on literary adventures with our
        curated book reviews, and reading recommendations, guiding you through the boundless
        worlds of fiction and non-fiction alike.</p>
</div>

<img src="/img/book-hero.png" alt="Aesthetic book image" class="hero-image" width="560" height="328">

<section class="articles">
    <h2 class="articles__heading">Latest Posts</h2>

    <ul class="article-ul">
        <% data.forEach(post => { %>
        <li>
            <a href="/post/<%= post._id %>">
                <span><img src="img/<%= post.image %>" style="height: auto; width: 50px;"></span>
                <span ><%= post.title %></span>
                <span class="article-list__date"><%= post.createdAt.toDateString() %></span>
            </a>
        </li>
        <% }) %>
    </ul>
    
    <% if (nextPage !== null) { %>
        <a href="/?page=<%= nextPage %>" class="pagination">&lt; View Older Posts</a>
    <% } %>
    
</section>